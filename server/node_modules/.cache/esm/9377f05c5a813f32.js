let Message;_5a8‍.x([["message",()=>message],["convMessages",()=>convMessages]]);_5a8‍.w("../models/message",[["default",["Message"],function(v){Message=v}]]);

       const message = async (req, res) => {
  const newMessage = new Message(req.body);
  try {
    const savedMessage = await newMessage.save();
    res.json(savedMessage);
  } catch (err) {
    _5a8‍.g.console.log(err);
  }
};

       const convMessages = async (req, res) => {
  try {
    const messages = await Message.find({
      conversationId: req.params.conversationId,
    });
    res.json(messages);
  } catch (err) {
    _5a8‍.g.console.log(err);
  }
};
